import React from 'react';

//Header is just a static display at the top of the view(rendered through App)
const Header = () => {
    return (
        <header>
            <h1>Gotta Catch em All!</h1>
        </header>
    )
}

export default Header;